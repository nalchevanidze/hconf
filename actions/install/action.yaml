name: Install HConf
description: Install and setup HConf CLI tool

runs:
  using: "composite"
  steps:
    - shell: bash
      run: curl -sSL https://raw.githubusercontent.com/nalchevanidze/hconf/main/scripts/install.sh | bash

